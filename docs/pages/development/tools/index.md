# Tools

Tools are go-scripts that help your local development. They will not be bundled to ldap-password-change.

## Requirements

- Create a folder under `tools`
- Exclude it from go build by adding `//go:build ldap-password-change_tools` as first line
