#!/bin/sh

files_changed=$(gofmt -l -s -w .)

if [ -z $files_changed ]
then
	echo "✅ Files correctly formatted"
	exit 0
else
	echo "❌ Files changed by pre-commit hook, please check, add and commit again"
	exit 1
fi
